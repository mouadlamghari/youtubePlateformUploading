export enum Actions{
    upload = 'upload',
    update = 'update', 
}

export enum status {
    s1 = "private",
    s2 = "public",
    s3 = "unlisted"
}

export enum comfirm {
    accept = "confirm",
    refuse = "refuse"
}
